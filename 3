def maximo(x,y):
    if x > y:
        return x
    else:
        return y
def minimo(x,y):
    if x<y:
        return x
    else:
        return y

# orograma pricinpal
x = int(input("un numero; "))
y = int(input("otro número: "))
print(maximo(x - 3, minimo(x + 2, y - 5)))
